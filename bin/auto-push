#!/usr/bin/env node

var autoPush = require('../index.js');

var fromPort = process.argv[2];
var toPort = process.argv[3];

if(!fromPort) {
  throw 'port is required';
}

autoPush.proxy(fromPort, toPort);
autoPush.standAlone(fromPort);